/**
 * Stores shader source code
 */
pub struct ShaderStore {
    shader_names: &[&str],
}

/**
 * Manages the maintenance (listening and loading) of shader code
 */
// impl ShaderStore {
//     pub fn new(shader_names: &[&str]) -> self {
//         Self { shader_names }
//     }

//     pub fn compile_shaders(&mut self, device: &wgpu::Device) {}
// }
